import { UseMountedDirective } from './use-mounted.directive';

describe('UseMountedDirective', () => {
  it('should create an instance', () => {
    const directive = new UseMountedDirective();
    expect(directive).toBeTruthy();
  });
});
