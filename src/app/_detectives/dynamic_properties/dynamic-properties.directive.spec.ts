import { DynamicPropertiesDirective } from './dynamic-properties.directive';

describe('DynamicPropertiesDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPropertiesDirective();
    expect(directive).toBeTruthy();
  });
});
