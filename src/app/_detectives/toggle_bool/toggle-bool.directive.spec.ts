import { ToggleBoolDirective } from './toggle-bool.directive';

describe('ToggleBoolDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleBoolDirective();
    expect(directive).toBeTruthy();
  });
});
